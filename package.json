{
  "name": "pi-fleet",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/unified-agent",
    "extensions/core",
    "extensions/fleet"
  ],
  "scripts": {
    "postinstall": "npm run build -w packages/unified-agent"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
